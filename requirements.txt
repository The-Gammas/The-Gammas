# Python dependencies for The Gammas — HCP N-back working-memory project.
#
# Version floors are deliberately loose so this installs cleanly on Google Colab
# and on local venvs alike. Pin harder only if we hit a real incompatibility.
# If a fresh install stops reproducing pipeline/02, fall back to the exact pins in
# requirements-lock.txt.
#
# Known-good reference (tested locally, Jul 2026):
#   python 3.12 · numpy 2.5 · pandas 3.0 · matplotlib 3.11 · scipy 1.18 ·
#   scikit-learn 1.9 · seaborn 0.13 · nilearn 0.13 · plotly 6.8 · jupyterlab 4.6

# --- core: used by gammas/ and by every notebook ---
numpy>=1.26
pandas>=2.1
matplotlib>=3.7
jupyterlab>=4.0

# --- data access: official NMA HCP loaders (download + atlas plotting) ---
requests>=2.31
nilearn>=0.10

# --- analysis: functional connectivity, metrics, prediction, figures ---
scipy>=1.11
scikit-learn>=1.3
seaborn>=0.13
plotly>=5.20        # interactive 3D brain map (pipeline/04)
