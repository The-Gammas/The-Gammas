# 📋 The Gammas — Meeting Minutes

**Date:** Tue 14 Jul 2026 (W2D2, project block) · **Attendees (confirmed):** Goutham, Jaime, Valeria, Kerem
Goutham walked through his analysis notebook (`sandbox/goutham/pre_analysis.ipynb` — FC + triple-network + CV prediction, currently on dataset A); the rest of the meeting was dataset and target.

> Provisional until the abstract (Fri 17 Jul). Tags: **decided / proposed / open**.

## Decided
- **Dataset — try B (336, `load_hcp`) and compare against A (100).** Driven by overfitting risk on 100 subjects. A stays as a validated fallback; formal choice pending the empirical comparison (`sandbox/jaime/03_dataset_comparison.ipynb`).
- Everyone keeps exploring in their own `sandbox/`.
- Unchanged from 10 Jul: weighted-undirected graphs · prediction on held-out subjects (CV) · roles · PTN excluded.

## Proposed (to confirm)
- **Target: d′ instead of raw 2-back accuracy (`acc_2bk`)** — Valeria's proposal; isolates sensitivity from response bias. **Only computable on B** (its `wm.csv` has consistent `ACC_TARGET` / `ACC_NONTARGET`; A's `Stats.txt` fields are internally inconsistent — known HCP issue). Helper: `sandbox/jaime/preprocessing.py → signal_detection_table`. Extreme-rate correction still to prespecify.

## Open — needed for the abstract (Fri 17)
- ❗ **A single falsifiable hypothesis — still not fixed.** Top priority.
- Confirm dataset once Goutham runs B; confirm target (acc_2bk vs d′).
- Resting-state / Objective 2: B reopens it as a possible extension (not MVP) — to decide.

## Next
Possible follow-up session **Wed 15 / Thu 16** · **Abstract due Fri 17 Jul.**
