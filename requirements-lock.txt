# Exact environment behind the canonical result — use only when reproducing it.
#
# requirements.txt carries loose floors so the repo installs anywhere (Colab
# included) and is what you normally want. This file is its opposite: the pinned
# fallback for when a fresh install no longer reproduces pipeline/02.
#
#   pip install -r requirements-lock.txt
#
# Interpreter: CPython 3.12.13 (macOS arm64, conda-forge build).
# Scope: only the packages this repo actually imports — this is a filtered lock,
# not a dump of the ~160 packages in the source environment.
#
# The five numeric pins are the versions stamped by the canonical run itself
# (pipeline/02 cell 32: 342.8 s, 1000 permutations, r = 0.3664 ± 0.0237); the
# tooling pins are read from that same interpreter. Provenance and the re-run of
# 25 Jul 2026 → docs/final-report.md §5 and §8.

# --- numeric path: everything in docs/final-report.md §5 comes out of these ---
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0
pandas==3.0.3
matplotlib==3.11.0

# --- tooling: runs the notebooks, fetches the data; no effect on the numbers ---
jupyterlab==4.6.2
requests==2.34.2

# nilearn, seaborn and plotly are NOT pinned here: they are absent from the
# interpreter snapshot (they only serve the brain-surface and interactive figure
# cells, never a reported number), so no verified version exists to record. Take
# them from requirements.txt.
#
# Drift already observed in that interpreter on 25 Jul 2026: pandas 3.0.5 and
# matplotlib 3.11.1 are installed there today. The pins above are the canonical
# run's versions, which is what a lock is for.
